info:
  name: Update Simulation Speed - Failure (Wrong Request Type)
  type: http
  seq: 7

http:
  method: GET
  url: "{{simulationUrl}}/speed?timeMultiplier=5"
  params:
    - name: timeMultiplier
      value: "5"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should reject GET method on update simulation speed endpoint", () => {
          expect(res.status).to.equal(405);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
