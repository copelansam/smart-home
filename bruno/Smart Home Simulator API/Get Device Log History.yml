info:
  name: Get Device Log History
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/devices/{id}/history
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
