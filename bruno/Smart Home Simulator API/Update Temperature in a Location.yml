info:
  name: Update Temperature in a Location
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/simulation/location/temperature
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
