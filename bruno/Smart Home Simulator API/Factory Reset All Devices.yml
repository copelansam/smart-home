info:
  name: Factory Reset All Devices
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/api/simulation/reset
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
