info:
  name: Delete Device
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/devices/{id}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
