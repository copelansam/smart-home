info:
  name: Get Single Device - Success
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/devices/{{deviceUuid}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should retrieve a single device", () => {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("object");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
