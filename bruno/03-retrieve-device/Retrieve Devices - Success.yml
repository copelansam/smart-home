info:
  name: Retrieve Devices - Success
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/devices"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should retrieve all devices", () => {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
