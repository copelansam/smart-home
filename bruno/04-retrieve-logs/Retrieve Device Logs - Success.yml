info:
  name: Retrieve Device Logs - Success
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/devices/{{deviceUuid}}/history"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Should retrieve all logs for the specified device", () => {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
